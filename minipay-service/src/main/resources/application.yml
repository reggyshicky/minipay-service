spring:
  application:
    name: minipay-service

  profiles:
    active: dev

  jackson:
    default-property-inclusion: non_null
    date-format: yyyy-MM-dd HH:mm:ss

server:
  port: 3271
  error:
    include-message: always
    include-binding-errors: always

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    root: INFO
    com.minipay: DEBUG
    org.hibernate.SQL: DEBUG

jwt:
  secret: ${JWT_SECRET:sjsjwjnbeekjwkejwwppxzakjkjdevklwkwlwiuu}
  expiration: 86400000 # 24 hours in ms

sms:
  provider: console
  africastalking:
    api-key: ${AT_API_KEY}
    username: ${AT_USERNAME:sandbox}

resilience4j:
  circuitbreaker:
    instances:
      paymentGateway:
        register-health-indicator: true
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 3
        automatic-transition-from-open-to-half-open-enabled: true
  retry:
    instances:
      paymentGateway:
        max-attempts: 3
        wait-duration: 500ms
        retry-exceptions:
          - com.minipay.minipay_service.exception.PaymentProcessingException
        ignore-exceptions:
          - com.minipay.minipay_service.exception.UserAlreadyExistsException

